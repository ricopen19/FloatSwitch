# FloatSwitch 設計仕様書

最終更新: 2026-02-24（スケッチ反映）

---

## 1. 概要

### 1.1 背景・課題

macOS 標準の `Command + Tab` によるウィンドウ切り替えには以下の問題がある。

- キーボードのみで完結しない（Enter キーで切り替えられない）
- 最小化ウィンドウが視認しづらく、切り替えに手間がかかる
- 操作のたびにマウス移動が発生し、作業フローが途切れる

### 1.2 コンセプト

**「常に最前面のミニマルなフローティングタブバー」**

`Command + Tab` の視覚的な代替として、画面端に常駐するフローティングバーを提供する。
アイコンクリックまたはホットキーで瞬時にウィンドウを切り替えられ、マウス移動を最小限に抑える。

---

## 2. 技術スタック

| 項目 | 採用技術 |
|------|----------|
| 言語 | Swift |
| UI フレームワーク | SwiftUI |
| プラットフォーム | macOS ネイティブ |
| 配布形態 | 公開予定（App Store または直接配布、未確定） |
| 最低対応 macOS バージョン | 未定 |

---

## 3. UI / UX 仕様

### 3.1 フローティングバー

- 常に最前面に表示（他のウィンドウに隠れない）
- **デフォルト位置**: 画面右下
- **移動・変形**: ドラッグで画面内の任意の位置に移動可能
- **向き**: 横置き・縦置きの切り替えに対応（右クリックメニューから選択）
  - ~~3次元回転は見送り（複雑さに対して効果が薄いため）~~
- **全体サイズ**: ユーザーが調節可能（詳細は未設計）

### 3.2 レイアウト・形状

2段構成でアプリとフォルダを分離する。バー下部は**湾曲した弧状**のシェイプ（Dock 風）。

```
  [Icon] AppA    [Icon] AppB  ...    ← 上段: アプリ
  [Icon] FolderA [Icon] FolderB ...  ← 下段: Finder フォルダ
╰─────────────────────────────────╯  ← 下部が湾曲した弧状
```

- 各アイテムはアプリアイコン + アプリ名（フォルダ名）を**常時表示**（ホバー不要）
- ホバーしたアイコンが**拡大表示**される（macOS Dock の拡大エフェクトに近い挙動）

### 3.3 外観カスタマイズ

- **グラデーション背景**: ホバー時にバー背景にグラデーションを表示（オプション）
  - プリセット: なし / ピンク / ブルー / パープル / オレンジ / ミント
  - 強度スライダー（0〜100%）で濃さを調整可能
  - 設定画面（SettingsView）の「外観」セクションで設定

### 3.4 非アクティブ時の挙動

- カーソルがバー外に出ると**半透明**になる
- バーのサイズや位置は変化しない（折りたたみなし）

---

## 4. 機能仕様

### 4.1 表示対象

| 種別 | 対象 |
|------|------|
| アプリ（上段） | 現在起動中の全アプリ（最小化含む） |
| フォルダ（下段） | Finder で開いているウィンドウ（最小化含む） |

### 4.2 ウィンドウ切り替え

- アイコンをクリックするとそのウィンドウをフォーカス・最前面に呼び出す
- 最小化されているウィンドウも復元してフォーカスする

### 4.3 複数ウィンドウの扱い

同一アプリが複数ウィンドウを持つ場合：

- バー上のアイコンは1つにまとめる
- クリック時にサブメニューを表示し、ウィンドウを選択して切り替える

### 4.4 ホットキー

- `Command + Tab` の代替として機能することを目的とする
- **デフォルト**: `Cmd + 数字（1〜9）` で上段アプリを**左から順**に切り替え（左端 = 1）
- ホットキーはユーザーがカスタマイズ可能
- ホットキーカスタマイズ機能はアプリ内の設定画面から行う（詳細は未確定）

---

## 5. 未確定・検討中の事項

| 項目 | 状況 |
|------|------|
| 最低対応 macOS バージョン | 未定 |
| 配布形態（App Store / 直接配布） | 未定 |
| バーの最大幅・スクロール動作（アプリが多い場合） | 未設計 |
| 起動時の自動起動設定 | 未設計 |

---

## 6. 非機能要件

- 作業領域を極力妨げないこと（省スペース設計）
- 常時起動を前提とした低リソース消費
- 公開を前提とした品質・安定性
