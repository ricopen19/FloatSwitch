# FloatSwitch タスク一覧

最終更新: 2026-02-27

---

## 凡例

| 記号 | 意味 |
|------|------|
| `[ ]` | 未着手 |
| `[~]` | 進行中 |
| `[x]` | 完了 |

---

## フェーズ 1: プロジェクトセットアップ

- [x] GitHub リポジトリ作成（private）
- [x] 設計仕様書作成（`docs/design.md`）
- [x] タスク一覧作成（`docs/tasks.md`）
- [x] Xcode プロジェクト作成（SwiftUI / macOS App）
- [x] 基本ディレクトリ構成の整備

---

## フェーズ 2: フローティングウィンドウの基盤

- [x] `NSPanel` で常に最前面に表示するウィンドウを実装
- [x] デフォルト位置を画面右下に設定
- [x] ドラッグで画面内の任意の位置に移動できるようにする
- [x] 非アクティブ時（カーソルがバー外）に半透明化する

---

## フェーズ 3: アプリ・フォルダの検出

- [x] `NSWorkspace` で起動中の全アプリ一覧を取得する
- [x] 最小化されているウィンドウを含めて取得する（起動中アプリは全件取得済み、per-window は Phase 5 で Accessibility API）
- [x] Finder で開いているウィンドウ（フォルダ）を取得する（ScriptingBridge）
- [x] アプリ・フォルダ情報をリアルタイムで更新する（アプリ起動・終了の検知）

---

## フェーズ 4: UI の実装

- [ ] 2段レイアウトの実装（上段: アプリ、下段: フォルダ）
- [ ] バー下部の湾曲した弧状シェイプを実装
- [ ] アイコン + アプリ名（フォルダ名）の常時表示
- [ ] ホバー時のアイコン拡大エフェクトの実装（Dock 風）
- [ ] バー全体のサイズ調節機能の実装

---

## フェーズ 5: ウィンドウ切り替え機能

- [ ] アイコンクリックでウィンドウをフォーカス・最前面に呼び出す
- [ ] 最小化ウィンドウを復元してフォーカスする
- [ ] 同一アプリが複数ウィンドウを持つ場合にアイコンを1つにまとめる
- [ ] クリック時にウィンドウ選択サブメニューを表示する

---

## フェーズ 6: ホットキー

- [ ] グローバルホットキー `Cmd + 数字（1〜9）` で左から順にアプリを切り替える
- [ ] ホットキーカスタマイズ用の設定画面を実装する

---

## フェーズ 7: 3次元回転・変形（最終フェーズ）

> 回転ジェスチャー割り当て・角度制限の詳細は実装フェーズで設計する

- [ ] 3次元回転（X / Y / Z 軸）の実装
- [ ] 横置き・縦置きの切り替えに対応する
- [ ] 任意角度への傾斜に対応する
- [ ] 回転操作のジェスチャー割り当てを設計・実装する
